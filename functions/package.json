{
  "name": "parkside-invitation-functions",
  "version": "1.0.0",
  "description": "Firebase Functions for Parkside Invitation (OG image generator)",
  "main": "og.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "start": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0",
    "sharp": "^0.32.0"
  }
}
